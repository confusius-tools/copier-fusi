[paths]
# Root directory containing the raw data.
data_root = "{{ data_root }}"

# Directory where results and figures are written.
results_root = "{{ results_root }}"

# TODO: Add project-specific path keys here as needed.

# TODO: Add additional sections such as [preprocessing] or [analysis] to match
# new grouped config dataclasses in src/{{ package_name }}/config.py.
