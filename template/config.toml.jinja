[paths]
# Root directory containing the raw data.
data_root = "{{ data_root }}"

# Directory where results and figures are written.
results_root = "{{ results_root }}"

# TODO: Add project-specific paths here, and wire them up in src/{{ package_name }}/config.py.
